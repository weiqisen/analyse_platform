# 可选：只有要改地址/口令时才复制成 .env（cp .env.example .env）。
# 不建 .env 也能直接 docker compose up -d —— compose 里已内置强口令默认值。

# 现场地址（一般需要改）
CS_URL=
WORKFLOW_URL=http://10.10.52.127

# 口令（默认已是强口令，改了 MQ 口令要同步告诉 CubeStudio）
DB_PASSWORD=Wqs@Defect2026#Db
MQ_USER=defect
MQ_PASS=Wqs@Defect2026#Mq

# 平台登录：admin / Wqs@Defect2026 （首次启动空库时建，可在「用户管理」改）
